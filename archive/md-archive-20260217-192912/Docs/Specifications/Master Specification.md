📘 Master Specification
Virtual Store–Driven Theme Development System
0. الهدف من هذا المستند

هذا المستند يعرّف النظام كاملًا من حيث:

الرؤية

المفاهيم الأساسية

المكونات

الطبقات

منطق العمل

التوسّع

طريقة التشغيل

بحيث يستطيع أي مبرمج:

فهم النظام
تحديد الوحدات
البدء بالتنفيذ مباشرة

1. تعريف النظام (System Definition)

النظام هو بيئة تطوير وتشغيل للثيمات تعتمد على متجر افتراضي يقوم مقام المتجر الحقيقي في التأثير على الثيم، ويتيح:

التحكم في إعدادات الثيم

التحكم في تركيب الصفحات والمكونات

التحكم في محتوى العرض

معاينة الثيم كما سيعمل بعد تركيبه فعليًا

التعديل المتوازي عبر الواجهة أو عبر الكود

2. المبادئ الأساسية (Core Principles)

الثيم لا يعمل منفردًا، بل دائمًا داخل سياق متجر

المتجر الافتراضي هو مركز القرار

ملفات الثيم هي مصدر القدرات فقط

محتوى المتجر وإعداداته تحدد السلوك الفعلي

لا يُعاد تنفيذ منطق المنصة

النظام يوفّر بيئة تشغيل واقعية للمعاينة فقط

3. المفاهيم الأساسية (Key Concepts)
3.1 Theme

مجموعة ملفات (Twig / Assets / Config)

يعرّف:

ما يمكن عرضه

ما يمكن تخصيصه

لا يحتوي على بيانات متجر حقيقية

3.2 Virtual Store

كيان داخل النظام يمثل متجرًا واحدًا.

يمتلك:

إعدادات الثيم

محتوى العرض

تخصيص الصفحات

تركيب المكونات

سيناريوهات تشغيل

3.3 Runtime Context

ناتج دمج:

قدرات الثيم

حالة المتجر الافتراضي

وهو المدخل الوحيد للمعاينة.

3.4 Scenario

حالة مختلفة لنفس المتجر، مثل:

متجر ممتلئ

متجر جديد

متجر عروض

متجر متعدد اللغات

4. البنية المعمارية (Architecture)
Theme Files
     │
     ▼
Virtual Store
     │
     ▼
Runtime Context Builder
     │
     ▼
Preview Runtime
     │
     ▼
Validation & Export

5. الطبقات بالتفصيل
5.1 Theme Files Layer

الوصف
تمثّل الثيم كما هو مكتوب.

تحتوي

قوالب العرض

الأصول

تعريفات العقد

ملاحظات تنفيذ

لا تُخزَّن في قاعدة البيانات

تُقرأ مباشرة من filesystem

أي تعديل يُعاد تحميله فورًا

5.2 Virtual Store Layer (الطبقة المركزية)

الوصف
تمثّل المتجر بوصفه الفاعل الرئيسي في تغيير سلوك الثيم.

تتكوّن من

Store Metadata

Theme Settings State

Page Composition

Component Instances

Store Content

Scenarios

يجب أن تكون

مستقلة

قابلة للتعدد

معزولة عن الثيم

5.3 Runtime Context Builder

الوصف
محرك دمج يبني سياق التشغيل.

المدخلات

Theme Definition

Virtual Store State

المخرجات

Runtime Context Object

مبدأ العمل

لا منطق أعمال

لا تفسير منصة

دمج مباشر فقط

5.4 Preview Runtime

الوصف
بيئة تشغيل للعرض والمعاينة.

تشمل

Twig Renderer

Live Reload

Scenario Switcher

Page Preview

خصائص

Stateless

تعتمد فقط على Context

5.5 Validation & Export

الوصف
مرحلة التحقق والتجهيز.

تشمل

فحص اكتمال التعريفات

فحص التوافق

توليد تقارير

تجهيز حزمة التصدير

6. طبقة الواجهة (UI Layer)

الغرض
تمكين التحكم بدون لمس الكود.

الوحدات

Theme Selector

Virtual Store Manager

Settings Editor

Page Composer

Component Editor

Content Manager

Live Preview

مبدأ
الواجهة لا تنفّذ منطق، بل تعدّل حالة المتجر فقط.

7. طبقة البيانات (Data Layer)
يُخزَّن

Virtual Stores

Store States

Scenarios

Snapshots

Validation Results

لا يُخزَّن

ملفات الثيم

القوالب

الأصول

8. التوسّع (Scalability)

النظام يدعم:

عدد غير محدود من المتاجر الافتراضية

تشغيل نفس الثيم على متاجر متعددة

تشغيل متاجر متعددة على نفس الثيم

مقارنة نتائج المعاينة

بدون تغيير معماري.

9. التحكم المزدوج (Dual Control)
عبر الواجهة

إعدادات

محتوى

تركيب صفحات

عبر الكود

تعديل الثيم

إعادة تحميل فوري

كلاهما يعملان على نفس الحالة.

10. دورة العمل (Workflow)

تحميل ثيم

إنشاء متجر افتراضي

ضبط الإعدادات

إدخال المحتوى

بناء السياق

المعاينة

التعديل

التحقق

التصدير

11. ما لا يفترضه النظام

لا يفترض منطق منصة

لا يتصل بمتاجر حقيقية

لا ينفّذ طلبات أو مدفوعات

لا يفرض طريقة عمل واحدة

12. نتيجة متوقعة من التنفيذ

بيئة تطوير ثيمات واقعية

تقليل الاعتماد على متاجر ديمو

تسريع تصميم وبناء الثيمات

رفع جودة المعاينة قبل النشر

13. تعريف نهائي مختصر (للملخص التنفيذي)

نظام لتطوير ومعاينة الثيمات يعتمد على متجر افتراضي يتحكم في إعدادات الثيم ومحتوى عرضه، مع دعم التعديل المباشر على ملفات الثيم، لإنتاج معاينة تشغيلية مطابقة لما سيحدث بعد التركيب الفعلي.
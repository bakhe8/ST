📜 التثبيت الرسمي — التحكم المزدوج في الثيم والمتجر
العنوان

Dual-Control Theme Development Model

حالة المستند

معتمد رسميًا (Ratified)

النص المعتمد

يعتمد هذا النظام نموذج تشغيل يتيح التحكم الكامل في الثيم ومحتوى المتجر الافتراضي لدعم عرض وتطوير الثيمات، مع السماح المتوازي بالتعديل المباشر على ملفات الثيم عبر الكود.

يُنفّذ هذا النموذج عبر ركيزتين متكاملتين:

التحكم عبر المتجر الافتراضي
يمكّن النظام من إدارة إعدادات الثيم، تركيب المكونات، تخصيص الصفحات، وإدارة المحتوى (منتجات، تصنيفات، نصوص، وسائط) بما يخدم عرض الثيم ومعاينته كما سيظهر عند التشغيل الفعلي.

التحكم عبر الكود
يمكّن النظام من تعديل ملفات الثيم مباشرة (قوالب العرض، الأصول، تعريفات العقد)، مع إعادة تشغيل المعاينة تلقائيًا وربط التغييرات بحالة المتجر الافتراضي دون تعارض.

المبدأ التشغيلي

الثيم يحدد قدرات العرض.

المتجر الافتراضي يحدد اختيارات العرض.

النظام يربط الاثنين لإنتاج سياق تشغيل ومعاينة متطابق.

التكامل التشغيلي
Theme Files (Code) +
Virtual Store (Settings + Content)
↓
Runtime Context
↓
Live Preview

الأثر على التنفيذ

يعتمد النظام هذا النموذج كأساس لكل:

المعاينة

الاختبار

التحقق

التصدير

لا يُفترض أسلوب عمل واحد؛ يُدعم العمل عبر الواجهة أو عبر الكود أو كليهما معًا.

تُدار حالة التشغيل كوحدة واحدة متسقة عبر جميع التغييرات.

الخلاصة المعتمدة

النظام يوفّر بيئة تشغيل للثيمات تعتمد على متجر افتراضي قابل للتخصيص،
مع دعم التعديل المباشر لملفات الثيم،
بما يضمن التحكم في سلوك الثيم ومحتوى عرضه عبر الإعدادات أو الكود دون تعارض.
